# Turso / libSQL. Do not commit real values.
# Local CLI: copy to .env and .env.production (gitignored).
# Git → Cloudflare Pages: set the same names as Production + Preview
# environment variables in the Pages project (needed at build time).
DB_URL=libsql://YOUR-DB-NAME-YOUR-ORG.turso.io
DB_KEY=YOUR_TURSO_AUTH_TOKEN
